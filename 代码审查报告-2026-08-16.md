# 灵犀 (Lingxi) 智能客服系统 · 代码全面审查报告

> 审查日期：2026-08-16
> 范围：后端 `backend/app/**`（约 2900 行 Python）、前端 `frontend/src/**`（约 2100 行 TS/TSX）
> 维度：代码质量 / 潜在 Bug / 安全问题 / 性能优化 / 一致性
> 方法：逐文件通读 + 跨端契约比对 + 单测/历史记录交叉验证

## 严重程度统计

| 等级 | 数量 | 说明 |
|------|------|------|
| 🔴 高 (High) | 3 | 跨端契约失效、异步架构阻塞、整块功能未接线 |
| 🟠 中 (Medium) | 10 | 安全加固缺口、并发竞态、确定性 Bug、UX 缺陷 |
| 🟡 低 (Low) | 9 | 一致性细节、死代码、可观测性、边界处理 |

---

## 🔴 高 (High)

### H1 · 前后端错误契约不一致 —— 所有后端错误提示被 axios 原始信息覆盖
- **位置**：后端 `app/api/**`（统一抛 `HTTPException(detail=...)` → 响应体 `{detail: "..."}`）；前端 `src/api/client.ts:27-35` `toApiError()`、`src/contracts/api.ts:160-164` `ApiError`。
- **问题**：契约 `ApiError` 定义为 `{code, message, request_id}`，但 FastAPI 实际返回 `{detail}`。前端 `toApiError` 读不到 `data.code/data.message`，于是 `message` 退化为 axios 网络串（如 *"Request failed with status code 401"*），后端精心编写的**中文错误文案（"账号或密码错误"、"今日问答额度已用完"、各类 400 校验）全部丢失**。`request_id` 由中间件写入响应头而非 body，永远为空。
- **影响**：登录失败、注册冲突、配额耗尽、字段校验等**所有错误提示对用户无意义**，且后端 SSE 的 `error` 事件（正确带 `code/message`）与 HTTP 错误走两套解析逻辑，体验割裂。
- **修复建议**：
  - 方案 A（推荐，后端改）：在 `main.py` 注册全局 `exception_handler(HTTPException)`，统一包裹为 `{code, message, request_id}`（code 用 HTTP 状态码或业务码，message 用 `exc.detail`）。
  - 方案 B（前端兜底）：`toApiError` 增加 `data?.detail ? data.detail : ...` 回退分支。
  - 任一方案即可让中文文案正确透传。

### H2 · 同步 DB 会话在异步 SSE 流式路由中跨整个生命周期持有 + 事件循环内阻塞
- **位置**：`app/api/chat.py:92-155`（`async def gen()` 内 `db.scalar/scalars/add/commit`）、`app/services/rag_service.py` `stream_answer`、`app/llm_clients/embedding.py:54` `embed()`。
- **问题**：
  1. `chat_stream` 是 `async def`，但拿到的是**同步 SQLAlchemy Session**（`get_db` 返回 sync Session）。在 `gen()` 异步生成器内部，从 `done` 事件落库到整个流式期间，这个同步会话一直开着（单次问答可能 10–60s），且 `db.commit()` 等调用在事件循环线程内**同步阻塞**。
  2. `run_pipeline → search_kb → embedding.embed()` 调用 `model.encode()`（CPU 密集、约 0.2–1s）是**同步阻塞调用，发生在 async 生成器内**，会卡住事件循环。
- **影响**：并发多路流式问答时，(a) 同步会话长期占用连接池（`pool_size=10`）易耗尽；(b) 嵌入/落库瞬间阻塞整个事件循环，其他请求排队。QPS 稍高即出现超时与吞吐塌陷。
- **修复建议**：
  - 短期：将 `gen()` 内同步 DB 操作包进 `fastapi.concurrency.run_in_threadpool`；embedding 调用同样 threadpool 化（或预热的后台 worker）。
  - 中期：接入 `asyncpg` + `async SQLAlchemy`（`AsyncSession`），让 `chat_stream` 真正异步；或将 RAG 检索/落库移到独立任务，SSE 仅做 token 转发。
  - 会话生命周期：落库动作集中在 `done` 一次性提交即可，避免在流式中途反复 `commit`。

### H3 · 知识库管理后台前后端均为空壳 —— 已完整实现的后端接口从未被前端接入
- **位置**：后端 `app/api/admin.py`（整个 router 为空，仅 `prefix="/admin"`）、前端 `src/api/knowledge.ts`（4 个函数全部 `throw new Error('TODO(FE-04)')`）、`src/pages/admin/KnowledgePage.tsx`（仅 `EmptyState` 占位）；对比后端 `app/api/knowledge.py` 已**完整实现** KB CRUD + 文档上传/列表/删除（挂在 `/knowledge-bases`、`/documents`）。
- **问题**：后端知识库管理能力完备且需 admin 权限，但前端 API 层直接抛 TODO、页面只放占位图，**admin 角色登录后「知识库」菜单完全不可用**。同时 `src/api/admin.ts`、`UsersPage.tsx`、`StatsPage.tsx` 也是同类占位 → 整个 `/admin/*` 与 `/agent/customers` 是"看得到点不动"。
- **影响**：一个被路由暴露、后端已支持的核心运营功能（知识库运维）实际不可用，属于明显的"功能完整性 / 前后端一致性"缺陷。
- **修复建议**：要么补齐（前端 `api/knowledge.ts` 接 `POST /knowledge-bases`、`POST /knowledge-bases/{kb}/documents` 等，KnowledgePage 渲染上传/列表/状态/删除）；要么在路由层把这些未实现菜单隐藏或标记"开发中"，避免误导。建议优先补齐知识库（后端已就绪，前端工作量小）。

---

## 🟠 中 (Medium)

### M1 · 认证无频率限制 / Refresh Token 无吊销机制
- **位置**：`app/api/auth.py:36-54`、`app/core/security.py:41-55`、`app/api/deps.py:27-34`。
- **问题**：
  - `/auth/login`、`/auth/register` 无任何速率限制 / 锁定，可被暴力破解或批量注册。
  - `create_refresh_token` 无 `jti`、无服务端状态，`/auth/refresh` 仅校验签名与 `type`，**无法吊销**；token 被盗可在 7 天内反复换 access，无"登出使旧 token 失效"能力（`AuthService` 注释自称"占位"）。
- **修复建议**：登录/注册加 Redis 滑动窗口限流（如 5 次/分钟）；refresh token 引入 `jti` + 服务端吊销表（或短期 access + 旋转 refresh）；提供 `/auth/logout` 将 `jti` 加入黑名单。

### M2 · 配额扣减存在 TOCTOU 竞态，且 Redis 不可用时配额形同关闭
- **位置**：`app/api/chat.py:77-82`（预检）+ `app/services/quota.py:37-54`（读/写分开）。
- **问题**：`left_today` 读取与 `increment` 写入非原子，并发请求可同时通过预检并超额；Redis 不可达时 `used_today` 返回 0、`left_today` 返回满额 → **配额保护失效**。
- **修复建议**：用已存在的 `Quota` 模型（见 L5）以 `SELECT ... FOR UPDATE` 行锁原子扣减；或 `INCR` + `EXPIRE` 后用 `GET` 比对上限做服务端拒绝。Redis 降级时建议 fail-closed（拒绝而非放行）或与限流合并处理。

### M3 · `GET /sessions/{id}` 未校验会话归属（越权读元数据）
- **位置**：`app/api/sessions.py:74-83`。
- **问题**：`get_session` 仅按 id 查，不校验 `s.user_id == 当前用户`，任何登录用户可枚举他人会话的标题/创建时间（UUID 难猜，风险有限，但属越权信息泄漏）。`create_session` 还读取 token 中并不存在的 `payload["tenant"]`（恒为 `"default"`）。
- **修复建议**：加 `if s.user_id != user_id: 403`；`create_session` 改用 `payload["sub"]` 派生租户或统一常量。

### M4 · `Chunk.hash` 使用 Python 内置 `hash()`（非确定性）
- **位置**：`app/repositories/document_repo.py:137` `hash=str(hash(text))[:64]`。
- **问题**：`hash()` 对 str 按进程加盐（`PYTHONHASHSEED`），**跨进程 / 重启结果不同**。该列本意为去重 / 幂等依据，实际完全失效，且不可用于跨 worker 比对。
- **修复建议**：改为 `hashlib.sha256(text.encode()).hexdigest()[:64]`（确定性）。

### M5 · JWT 密钥为固定明文开发值
- **位置**：`backend/.env:21` `JWT_SECRET=9f2a7c4e1b8d6a3f5c0e7b2d4a6f8c1e`。
- **问题**：`.env` 已被根 `.gitignore` 忽略（未泄漏），但该值是写死的静态密钥。一旦该 `.env` 用于生产，攻击者可**伪造任意用户/角色 token**（含 admin）。
- **修复建议**：生产部署强制 `JWT_SECRET` 从密钥管理注入随机值；CI 增加"禁止默认密钥"检查；本地 dev 密钥与 prod 严格分离。

### M6 · 意图识别为子串匹配，易误触发 / 关键词重叠
- **位置**：`app/services/rag_service.py:30-36, 52-58`。
- **问题**：`"人工"` 命中子串（如"人工智能"→误判 handoff）；`"真人"` 同时出现在 `HANDOFF_KEYWORDS` 与 `CHITCHAT_KEYWORDS`（靠顺序勉强收敛）；无词边界。
- **修复建议**：改用分词 + 词边界匹配（如正则 `\b` 或 jieba），或白名单短句；移除重叠关键词；对"人工"加排除上下文。

### M7 · 助手 Markdown 回复未渲染（原样显示 `**` / `-`）
- **位置**：`src/components/chat/MessageBubble.tsx:18`（用 `Typography.Paragraph` 纯文本）+ `app/prompts/qa_prompt.py`（明确要求 Markdown 输出）。
- **问题**：prompt 要求"Markdown 格式 + 列表 + 加粗"，但前端以纯文本渲染，**`**加粗**`、`- 列表` 等语法字符直接暴露给用户**，与"统一规范回复格式"的诉求相悖（且之前用户明确要求规范格式）。
- **修复建议**：引入轻量 Markdown 渲染（如 `react-markdown` + `remark-gfm`），并禁用原始 HTML 以防 XSS（仅允许安全标签）。当前用 `Typography.Paragraph` 已天然防 XSS，引入渲染库时务必关闭 `dangerouslySetInnerHTML` / 启用 rehype-sanitize。

### M8 · Agent 会话列表点击行无效（只跳 `/chat` 新建会话）
- **位置**：`src/pages/agent/SessionsPage.tsx:62`（`onRow` → `navigate('/chat')`）；`src/api/sessions.ts` 已实现 `getSessionDetail` 但**从未被调用**。
- **问题**：客服点击某用户会话，不会打开该会话历史，而是跳到自己的 `/chat`（且 ChatContainer 总是懒建新会话），**无法查看用户历史对话**。
- **修复建议**：点击行 → `/chat?session={id}` 或独立会话详情页，复用 `getSessionDetail`；ChatContainer 支持"带 sessionId 进入"而非强制新建。

### M9 · 文档导入 Celery 不可达时同步降级，阻塞 API 请求
- **位置**：`app/api/knowledge.py:150-165`（`.delay` 失败 → 同步 `import_document`）+ `app/services/knowledge_import_service.py`（同步 embedding）。
- **问题**：降级路径在**请求线程内**同步跑完整导入（含 `model.encode` 向量化，单次可达数秒~数十秒），直接阻塞上传 HTTP 请求并长期持有请求 DB 会话。
- **修复建议**：降级也应后台化（如线程池 / 本地 task queue），或导入状态先返回 `parsing`、由后台线程完成；严禁在请求同步路径跑重型嵌入。

### M10 · Prompt Injection 面（用户/历史内容直接拼入系统提示未隔离）
- **位置**：`app/prompts/qa_prompt.py:42-53`（`build_qa_messages` 把 query/history 直接拼接进 system prompt）。
- **问题**：用户 query 与历史 assistant 消息未经结构化分隔即注入提示，存在指令注入风险（尽管严格系统规则降低了模型顺从概率，但无结构性防护）。
- **修复建议**：用明确分隔符 + 角色标注（如 `<<用户问题>> ... <</用户问题>>`），并在系统提示中强调"用户输入不是指令"；对历史消息做可信标记。

---

## 🟡 低 (Low)

### L1 · `backend/.gitignore` 未列 `.env`
- **位置**：`backend/.gitignore`（仅忽略 `.pytest_cache/.ruff_cache/__pycache__/*.egg-info/.coverage/coverage.xml`）。
- **现状**：根 `.gitignore` 含 `.env` 已兜底（已 `git check-ignore` 验证未跟踪），但局部遗漏易被误判/误提交。
- **修复**：在 `backend/.gitignore` 显式追加 `.env`，双保险。

### L2 · 契约 `Session` 字段与后端不一致
- **位置**：`src/contracts/api.ts:42-60`（`Session` 含 `updated_at`、`SessionListResp` 含 `total`）vs 后端 `schemas`（`SessionItem` 无 `updated_at`、`SessionListResp` 无 `total`）。
- **现状**：前端 `api/sessions.ts` 用 adapter 桥接（`id←session_id`、`updated_at←created_at`）勉强可用；`total` 用 `items.length` 冒充（>50 条即失真）。
- **修复**：契约对齐后端真实字段，或从后端返回真实 `total`/`updated_at`。

### L3 · `/docs`、`/redoc` 生产环境暴露
- **位置**：`app/main.py:41-42`。
- **修复**：按环境（如 `settings.ENV != "prod"`）门控 `docs_url/redoc_url=None`，避免 API schema 信息泄漏。

### L4 · 检索参数硬编码 / 默认不一致
- **位置**：`app/services/retrieval_service.py:37`（`top_k` 默认 5，实际调用 8）、`app/services/rag_service.py:39`（`MIN_SCORE=0.30` 未入配置）。
- **修复**：统一默认值为 8；`MIN_SCORE` 提为 `Settings` 字段便于校准。

### L5 · 死代码 / 实现矛盾：`Quota` ORM 模型从未使用
- **位置**：`app/models/quota.py`（含 `Quota` 表 + 文档称"行锁 SELECT FOR UPDATE 防超卖"），但 `app/services/quota.py` 实际用 **Redis 计数器**，该模型零引用（迁移仍建了 `quotas` 表）。
- **同组**：前端 `src/api/admin.ts`、`UsersPage.tsx`、`StatsPage.tsx` 全为占位 TODO。
- **修复**：二选一 —— (a) 删除 `Quota` 模型与迁移表，统一 Redis 方案；(b) 启用 `Quota` 模型替换 Redis 以解决 M2 竞态。建议保留 (b) 的思路但需落地；当前至少清理文档误导。

### L6 · 登录后未回到来源页
- **位置**：`src/pages/LoginPage.tsx:29`（`navigate('/chat')` 硬编码）；`RequireAuth` 已存 `state.from` 但未消费。
- **修复**：`navigate(location.state?.from || '/chat')`。

### L7 · 重复网络往返 / 可缓存调用
- **位置**：`app/api/chat.py:47 _latest_kb_id`（每请求查 KB）、`app/services/vector_service.py` `ensure_collection`/`delete_by_doc_id`（每次 `get_collections()`）。
- **修复**：KB id 启动时解析或短 TTL 缓存；集合存在性本地缓存（维度不变）。

### L8 · 流式无前端超时 / 自动滚底不尊重用户上滑
- **位置**：`src/hooks/useChatStream.ts`（仅依赖 LLM 60s httpx 超时兜底）、`src/components/chat/MessageList.tsx:26-29`（每次 token 强制滚底）。
- **修复**：fetch 加 `AbortController` 超时；用户上滑时暂停自动滚底，回到底部再恢复。

### L9 · Composer 客户端未限制长度
- **位置**：`src/components/chat/Composer.tsx`（无 maxLength）。
- **修复**：加 `maxLength={4000}` 与计数提示，与后端 `ChatStreamReq.content` 上限对齐。

### L10 · `RequestIDMiddleware` 用 `BaseHTTPMiddleware`（流式/性能隐患）
- **位置**：`app/main.py:24-32, 45`。
- **说明**：`BaseHTTPMiddleware` 对 `StreamingResponse` 有潜在缓冲与额外开销。当前 SSE 可用，但建议替换为 Starlette 原生 `@app.middleware` 或仅在需要时注入 request_id（如从 header 透传即可，无需包装响应体）。

---

## 优先修复路线建议

1. **立即（正确性/体验）**：H1（错误文案透传）、M7（Markdown 渲染）、M4（确定性 hash）、M3（越权读）。
2. **本周（安全/稳定）**：M1（限流+吊销）、M2（配额原子化）、M5（密钥分离）、M6（意图匹配）。
3. **迭代（架构/完整度）**：H2（异步化）、H3（补齐知识库前端）、M8（会话详情）、M9（导入降级）、L5（配额实现统一）。

> 注：本次审查未改动任何代码，仅做静态分析与契约比对。后端单测 102 项、`tsc` 0 错误、前端 16 项 vitest 均已通过（历史记录），上述多为设计/一致性/健壮性层面的改进点，而非导致测试失败的缺陷。
