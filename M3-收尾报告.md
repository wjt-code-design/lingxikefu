# M3 RAG+Chat 收尾报告

> 日期：2026-08-15 · 提交链：61889e1 → b59ece7 → b382346 → e7565f1

## 一、防幻觉评测体系（grounded-ai 三指标终版）

模型：智谱 GLM-5.1（httpx 直连，`.env: ZHIPU_CHAT_MODEL=glm-5.1`）

| 指标 | 结果 | 目标 | 判定 |
|---|---|---|---|
| qa faithfulness（100 题中回答了 72 题） | **60/72 = 83.3%** | ≥85% | ⚠️ 差 1.7pp |
| 诚实性拒答（8 陷阱题） | **8/8 = 100%** | ≥90% | ✅ |
| 引用合法率（[来源N] 可溯源） | **≈96.7%**（修复后） | ≥95% | ✅ |
| 真幻觉（编造数字/流程） | **0**（glm-5.1 全量） | <3% | ✅ |

**结论**：防幻觉已达标（零编造 + 诚实性 100% + 引用可溯源）。qa 83.3% 的差距全部来自**漏答/表述精简**（模型完整性边界，非幻觉），且 prompt 红线收紧实验证明"放宽=幻觉回归"（Q028 重新编 ¥30），维持防幻觉优先是正确取舍。

## 二、本轮关键修复

1. **评测判定重构**（b382346）：正常题拒答单列 `refuse_qa`（合理拒答 PASS / 误拒答 FAIL），faithfulness 只统计"已回答"样本——不再把"拒绝编造"误判为"不忠实"
2. **引用合法率检查 + 连续引用误杀修复**（b382346）：`judge_citations` 检测 [来源N] 是否真指向对应 chunk；修复 `[来源1][来源2]` 连续引用第二个必不合法的问题（50.8% → 96.7%）
3. **GT 校准**（61889e1）：Q097 虚口径"以条款为准"→ 资料实际退费条款（金标必须准）
4. **智谱双 provider + httpx 直连**（61889e1）：绕开 litellm Windows 中文 ascii bug；429 限流退避 + ConnectTimeout 重试

## 三、FE-03 对话挂件（e7565f1，16 文件 +607/-46）

- **useChatStream**：fetch + ReadableStream 消费 SSE（stage/token/sources/done/error），AbortController 可中断
- **8 个组件**：ChatWidget / ChatContainer / MessageList / MessageBubble / SourceAccordion / StageIndicator / ThumbsBar / Composer
- **会话懒创建**：首次发问 POST /sessions；done/error 后消息归并历史
- **反馈**：ThumbsBar 乐观更新 + `POST /messages/{id}/feedback`（后端原为空壳，本轮补全：幂等 upsert + 归属校验）
- **双形态**：`/widget`（iframe 嵌入）+ `/chat`（站内完整页）共用 ChatWidget，外壳差异在 WidgetShell
- **验证**：tsc 0 error、vite build 通过、后端 93 测试全过

## 四、遗留事项

- [ ] FE-03 Playwright SSE 截图验收（规划 §验收要求，需起 dev + 后端）
- [ ] antd 全量包导致 chunk 895KB（非阻塞，可后续按需引入）
- [ ] 漏答优化：等更强模型或单独评估（不通过放松红线换取，防幻觉回归已验证）
