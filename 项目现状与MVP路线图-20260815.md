# 灵犀 Lingxi · 项目现状与 MVP 实施路线图

> 版本：v0.1（2026-08-15 校准版，基于实际代码状态侦察，非规划书照抄）
> 依据：规划书 v0.2 + 后端/前端细规划 + 代码现状核查（agent 只读侦察 + git 历史）

---

## 一、已完成功能模块（基线已冻结）

### 后端（BU-01 ✅，commit 1ed18bd）
| 模块 | 状态 | 证据 |
|---|---|---|
| 应用骨架 | ✅ | `main.py`：挂载 7 个 router 至 `/api/v1`、`/health`、RequestID/CORS 中间件、启动时 `settings.validate()` |
| 配置单一真源 | ✅ | `core/config.py` Pydantic Settings + 启动校验（9 个单测覆盖） |
| 数据模型 | ✅ | 11 张表全定义（User/Session/Message/MessageSource/Feedback/Ticket/Quota/KnowledgeBase/Document/Chunk/ChunkContext），**全表含 tenant_id**（5 个单测验证） |
| 迁移 | ✅ | Alembic 首版迁移 `0001_initial` |
| JWT 工具 | ✅ 占位 | `core/security.py` 3 函数签名就位（BU-02 填充，docstring 已如实标注） |
| Celery | ✅ 骨架 | `workers/celery_app.py`（BU-04 填充导入任务） |
| 部署/CI | ✅ | Dockerfile + docker-compose + CI（ruff/pytest/secret 扫描/镜像构建） |

### 前端（FE-01 + FE-05 契约 ✅，commit ce8f149）
| 模块 | 状态 | 证据 |
|---|---|---|
| 脚手架 | ✅ | Vite + React19 + TS + AntD5 + Router + React Query + Zustand + 主题系统 |
| 布局壳 | ✅ 空壳 | AuthLayout / AdminLayout / WidgetShell 可路由切换 |
| 通用组件 | ✅ 真实 | AppHeader / SideNav / ErrorBoundary / EmptyState / LoadingSpin / RequireAuth |
| 状态管理 | ✅ 真实 | `authStore`（token 持久化）、`themeStore`（light/dark/system） |
| API 基础设施 | ✅ 真实 | `api/client.ts`：axios + Bearer 注入 + 错误归一化 |
| 契约单一真源 | ✅ | `contracts/api.ts` 31 项导出，与后端 schema 对齐；`api-schema.json` 双源 |
| SSE hook | 🟡 骨架 | `useChatStream` 状态机完整，`stream()` 抛 TODO（待 BU-06 后接 SSE 消费） |
| CI | ✅ | lint / typecheck / vitest / e2e(playwright) / build 五段式 |

### 语料与工程治理（✅）
- 评测库 80 问 + 11 篇 kb 文档（含 5 条行业通用政策）+ 3 个 PDF 夹具
- git worktree 派发流程、`.gitattributes` 统一 LF、前端 CI、后端覆盖率门禁 **89%**（≥70 达标）

---

## 二、当前进展量化（测试基线）

| 项 | 数值 |
|---|---|
| 后端单测 | 18 passed（config 9 / health 2 / models-tenant 5） |
| 后端覆盖率 | 89%（300 行 / 33 未覆盖；database、security、celery_app 为"接线未测"） |
| 前端单测 | 11 passed（2 文件） |
| 前端类型 | tsc --noEmit 0 错误 |
| 前端 e2e | login.spec.ts 用例已建（需后端 BU-02 真实可用才能跑绿） |
| 提交链 | master 9 commits（e4aff8c → 1248fd4） |

---

## 三、未完成与遗留问题（按优先级）

### P0 — 阻塞主线推进
| # | 问题 | 影响 |
|---|---|---|
| P0-1 | **阿里云百炼 API Key 未提供** | BU-04/05/06 的 LLM/embedding/rerank 真调全部阻塞，只能 mock 测 |
| P0-2 | 后端 7 个 API 全部 stub（`app/api/*.py` 0 个 def） | 无任何业务接口可用 |
| P0-3 | 后端 6 个子包空占位（llm_clients/orchestrator/prompts/rag_pipeline/repositories/retrieval/services） | 11 张表零接线 |
| P0-4 | 前端 7 页面空壳 + containers 空 + `components/chat/` 空 + 5 个 api client stub | 无业务页面可用 |

### P1 — 需尽快修正
| # | 问题 | 说明 |
|---|---|---|
| P1-1 | 规划文档 BU 依赖表笔误 | BU-04/BU-05 依赖写"BU-08(向量 client)"，实际 BU-08=Quota；向量 client 属 llm_clients 层，需修正以免派发误解 |
| P1-2 | docker-compose 端到端未真实验证 | BU-01 DoD 声明"compose up 起 5 容器"，但历史只有单测验证，未实跑 |
| P1-3 | 前端 e2e 依赖后端 | login.spec.ts 已建，需 BU-02 完成后才能全绿 |

### P2 — 收尾项
| # | 问题 | 说明 |
|---|---|---|
| P2-1 | 评测集 80 问未冻结 hash | 规划要求 W4 冻结基线（防 metric-gaming），BU-05 前需完成 |
| P2-2 | Phase2/3 未启动 | 正常，按规划在 MVP 验收后启动（工单/渠道/多租户） |

---

## 四、MVP 实施路线图（依赖驱动，双轨并行）

> 总目标：W1–W6 完成单租户 + 仅 Web + 百炼的 RAG 问答闭环（对应规划书⑦）。
> 现状 = W1 的脚手架部分完成，从"W1 剩余"接续。

### M1 认证与账户体系（≈W1 剩余）
- **任务**：BU-02 Auth（register/login/refresh/logout/me + JWT 中间件 + 三角色 + bcrypt）+ FE-02 认证页（Login/Register + 路由守卫 + token 刷新）
- **关键依赖**：BU-01 ✅（已完成）；需用户提供百炼 Key 前先用测试密钥跑单测（JWT 不依赖 LLM，**本阶段无 Key 可独立完成**）
- **预期交付**：注册登录拿 token、`/auth/me` 返回角色、越权 403；前端登录后跳转、未登录重定向；后端 5 端点单测绿 + 前端表单校验测试绿 + e2e login 全绿

### M2 知识库链路（≈W2–W3）
- **任务**：llm_clients 三 client（chat/embedding/rerank，经 LiteLLM→百炼）+ BU-04 Knowledge（上传 .txt/.md/**pdf**、解析、切片、向量化、sha256 去重、状态机、失败回滚、Celery 导入 worker）+ BU-08 Quota（每日配额行锁，**可提前到 M1 并行**）
- **关键依赖**：**P0-1 百炼 Key（embedding 真调必需）**；Qdrant + PostgreSQL 真实例（docker compose）；repositories 层开始接线
- **预期交付**：上传 50 篇（含 PDF）入库、sha256 重复跳过、PDF 解析成功（修 AegisDesk 坑）、失败回滚清向量；单测覆盖 txt/md/pdf + 回滚；配额并发无超卖

### M3 RAG 问答闭环（≈W3–W4，核心价值）
- **任务**：BU-05 RAG 管线（LangGraph 7 节点：intent→rewrite→hybrid retrieve→RRF→rerank→兜底过滤→expand→generate）+ BU-03 Sessions（会话 CRUD + 上下文）+ BU-06 Chat（SSE：stage/token/sources/done/error + 写 message_sources 真源 + 配额扣减）+ FE-03 对话挂件（ChatWidget + 流式渲染 + SourceAccordion + ThumbsBar）
- **关键依赖**：M2 完成（chunks 入库）、**百炼 Key（chat/rerank 真调）**、评测集 80 问冻结（P2-1）
- **预期交付**：SSE 事件顺序与契约一致、回答 100% 带 DB 来源、faithfulness>85% / recall@5>85%（评测集驱动）、rerank 失败自动降级、注入攻击被拦截；前端挂件流式光标 + 来源折叠 + 赞踩写库

### M4 运营闭环（≈W5–W6）
- **任务**：BU-07 Feedback（赞踩幂等，单真源 message_sources）+ BU-09 Admin（用户管理/知识库管理视角/**真实统计**禁空壳）+ FE-04 管理后台（KnowledgePage/UsersPage/StatsPage + 真实数据）
- **关键依赖**：M2/M3 的数据产物（documents/chunks/messages/feedback）
- **预期交付**：admin 能管用户/知识库；stats 返回与 DB 一致的真实数字；非 admin 403；前端三页真实数据渲染 + 上传状态机显示

### M5 MVP 验收（W6 末）
- **任务**：docker-compose 端到端实跑（api+worker+pg+qdrant+redis+frontend+litellm 网关）、CI 全绿（前后端）、评测集回归（RAGAS 基线冻结 hash）、覆盖率 ≥70% 复核
- **预期交付**：`docker compose up` 一键起、手机浏览器经内网穿透可访问、W6 DoD 全部打勾（对应规划书⑦）

---

## 五、关键依赖与风险

| 依赖项 | 类型 | 状态 | 不满足的后果 |
|---|---|---|---|
| 阿里云百炼 API Key | 外部凭证 | **未提供** | M2/M3 真调阻塞；只能 mock 验证（交付质量打折） |
| Qdrant + PostgreSQL 真实例 | 基础设施 | docker compose 已配未实跑 | 集成测试只能 fake，违反"真后端"纪律 |
| 评测集 80 问冻结 | 内部资产 | 已生成未冻结 | 指标可被 gaming，无法锁定基线 |
| 规划文档 BU 依赖表笔误 | 文档 | 未修 | 派发子 agent 可能误解依赖顺序 |

## 六、建议下一步动作（本周）

1. **确认百炼 Key 是否可提供** → 决定 M2/M3 是"真调开发"还是"mock 先行 + 后补真调"
2. **修正规划文档 P1-1 依赖笔误**（5 分钟，纯文档）
3. **启动 M1**：派发 BU-02 Auth + BU-08 Quota（均不依赖 LLM，可立即开工）+ FE-02 认证页
4. M2 前置：先跑通 docker-compose（验证 P1-2），再派发 llm_clients + BU-04
