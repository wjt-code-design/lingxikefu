# 智能 AI 客服系统（代号：灵犀 / Lingxi）规划书

> 版本：v0.2（决策收敛版，5 个承重分支已拍板）
> 编制：资深项目经理视角（SeniorProjectManager）
> 参考蓝本：[aegisdesk-ai](https://github.com/Itachi852/aegisdesk-ai)（已通读全部源码与文档）
> 定位：开工蓝图，供后续 `/module-dispatch-new` 派发、`/plan-backend-new`、`/plan-frontend-new` 细化使用。

---

## ① 目标与范围（为什么 / 做什么 / 不做什么）

### 1.1 项目背景
AegisDesk AI 已验证一条可行路径：**FastAPI + LangGraph + Qdrant + React/AntD 的 RAG 智能客服**，能把企业私有文档变成可对话检索的客服知识库。但它有硬伤（见⑨）：仅 1 次提交、无测试/CI、仅支持 `.txt/.md`、PDF 是 `NotImplementedError`、配置默认值三处自相矛盾、知识来源"模型手写 + source 事件"双轨脆弱、无多渠道、无工单。

本项目目标：**在 AegisDesk 已验证的 RAG 架构之上，补齐它缺失的生产级能力**——多渠道接入、工单流转、更稳的知识来源、从 day1 的工程纪律（测试 + CI），做成一套可私有化部署、可扩展到多租户的客服系统。

### 1.2 真实目标 vs 代理目标
- **真实目标**：降低人工客服重复咨询成本，把回答锚定在企业私有知识上以降幻觉，并在 AI 答不了时平滑转人工/工单。
- **代理目标（要警惕）**："做一个最先进的 Agent 平台""支持所有渠道和所有模型"——这是范围陷阱。MVP 不做花哨编排，先把单渠道 RAG 问答跑稳。

### 1.3 做什么（MVP 范围，已收敛）
- Web 嵌入式对话挂件 + 客服/管理员后台（React + AntD）。
- 知识库：上传 `.txt / .md / .pdf`，自动切片、向量化、入库，sha256 去重。
- RAG 问答：意图路由 → 多 query 改写 → 混合检索（dense+sparse）→ RRF 融合 → 重排 → 业务意图兜底过滤 → 流式回答。
- 多轮会话管理（独立会话、上下文、历史）。
- 基础管理：用户/知识库/配额（每日配额独立表 + 行锁）。

### 1.4 不做什么（范围红线）
- ❌ MVP 不做多租户 SaaS（单租户私有化，架构预留 `tenant_id`；多租户放 Phase 3）。
- ❌ MVP 不做多渠道（仅 Web 挂件；企业微信/邮件放 Phase 2）。
- ❌ 不做电话/语音客服。
- ❌ 不做自研大模型，只做模型接入网关（百炼为主，网关可切换）。
- ❌ 不做复杂 RBAC 细粒度（基础角色：管理员/客服/普通用户；细粒度放 Phase 2）。
- ❌ 不做 AegisDesk 那种空壳 `/admin/stats`——管理统计按需做，且必须有真实数据。

---

## ② 技术选型与理由（决策已定，进 ADR）

| 层 | 选型 | 理由 | 状态 |
|---|---|---|---|
| 后端框架 | **FastAPI** | AegisDesk 已验证，异步 + 类型 + SSE 原生友好 | ✅ 定 |
| 编排 | **LangGraph** | AegisDesk 用 7 节点状态机验证过；可视化、可断点、可观测 | ✅ 定 |
| 关系库 | **PostgreSQL 16** | 事务/行锁稳；全表预留 `tenant_id` | ✅ 定 |
| 向量库 | **Qdrant** | AegisDesk 已验证混合检索；独立扩展；payload 过滤干净 | ✅ **ADR-1 定** |
| 前端 | **React 19 + TS + Ant Design 5** | AegisDesk 验证；组件全、中文文档好 | ✅ 定 |
| 实时通道 | **SSE（问答流）+ WebSocket（在线/转人工，Phase2）** | SSE 已验证；WS 用于转人工信令 | ✅ 定 |
| 模型网关 | **LiteLLM** | 统一多厂商；默认百炼、可切换 | ✅ **ADR-3 定** |
| LLM 供应商 | **阿里云百炼（用户供 Key）** | 数据不出境、合规、中文强；chat=通义千问 / embedding=百炼 text-embedding / rerank=百炼 gte-rerank | ✅ **ADR-3 定** |
| 解析 | **pdfplumber + markdown 库 / unstructured** | 解决 AegisDesk 的 PDF `NotImplementedError` | ✅ 定 |
| 缓存 | **Redis** | 会话上下文、限流、配额计数 | ✅ 定 |
| 任务队列 | **Celery + Redis**（知识导入异步） | 大文件导入不阻塞主流程 | ✅ 定 |
| 可观测 | **OpenTelemetry + 日志** | 每条问答留 trace，便于评测/审计 | ✅ 定 |

**密钥约束（来自 ADR-3）**：百炼 API Key 经 LiteLLM 网关的 secret 管理（环境变量 / KMS）注入，**绝不写进 `.env.example` 或提交仓库**——直接规避 AegisDesk 把真实 Qdrant cloud endpoint 硬编码的坑。

---

## ③ 整体架构（模块划分 + 接口边界 + 依赖图）

```
                        ┌─────────────── 客户端 ───────────────┐
                  Web 挂件(React)   客服后台(React)   管理后台(React)
                        │  SSE / REST          │ WebSocket(Phase2: 在线/转人工)
                        └────────────┬──────────┴───────────────┘
                                     │  HTTPS
                        ┌────────────▼──────────┐
                        │   API Gateway / 鉴权   │  (FastAPI 中间件: JWT + 配额)
                        └────────────┬──────────┘
            ┌───────────────┬────────┴────────┬─────────────────┐
            ▼               ▼                 ▼                 ▼
     ┌────────────┐  ┌────────────┐   ┌────────────┐   ┌──────────────┐
     │ 对话服务    │  │ 知识库服务  │   │ 工单服务*   │   │ 渠道适配层*   │
     │(LangGraph) │  │(导入/检索)  │   │(流转/升级)  │   │(WS/邮件/IM)  │
     └─────┬──────┘  └─────┬──────┘   └─────┬──────┘   └─────┬───────┘
           │               │               │                │
     ┌─────▼──────┐  ┌─────▼──────┐  ┌─────▼──────┐   ┌─────▼───────┐
     │ 检索/重排   │  │ 向量库      │  │ 关系库      │   │ 模型网关     │
     │(RAG pipe)  │  │(Qdrant)    │  │(PostgreSQL)│   │(LiteLLM→百炼)│
     └────────────┘  └────────────┘  └────────────┘   └─────────────┘
           * = Phase 2 起
```

**依赖边界（硬规则）**
- 渠道适配层只能调「对话服务 / 工单服务」的公开 API，不得直连向量库。
- 所有不可信用户输入（用户消息、抓取网页）进入提示词前必须显式标注 + 转义（沿用 Customer-Agent 的注入防护）。
- LLM 调用一律经 LiteLLM 网关，禁止前端直连模型。
- 全表带 `tenant_id`，MVP 单租户（固定值），Phase 3 多租户时启用行级过滤。

---

## ④ 模块规划

### 4.1 后端模块（委托 `/plan-backend-new` 细化）
- **auth**：邮箱/手机号注册登录、JWT、Refresh；基础角色（管理员/客服/普通用户）。
- **sessions**：多轮会话 CRUD、上下文窗口管理、历史归档。
- **chat**：接收消息 → 调 LangGraph 编排 → SSE 流式回传；阶段进度事件（检索中/生成中）。
- **knowledge**：文档上传、解析（txt/md/**pdf**）、切片、向量化、sha256 去重、导入失败回滚（沿用 AegisDesk 的"清向量回滚"）。
- **rag**（核心管线）：意图路由 → 多 query 改写 → 混合检索 → 外层 RRF → 重排 → **业务意图关键词兜底过滤** → 相邻块扩展 → 生成。
- **quota**：每日提问配额独立表 + 行锁（沿用 AegisDesk 防绕过做法）。
- **feedback**：点赞/踩 + 知识来源标注（**统一真源为数据库 `message_sources`，废弃"模型手写 source"双轨**，修 AegisDesk 脆弱点）。
- **ticket\***（Phase 2）：AI 判定需转人工 → 建轻量工单 → 状态机（待处理/处理中/已解决/已关闭）→ 分配给客服；数据模型预留 `external_ref` + `TicketAdapter` 抽象接口，供以后对接飞书/Jira/TAPD。
- **channel\***（Phase 2）：WebSocket 在线信令 + 企业微信/邮件适配器。

### 4.2 前端模块（委托 `/plan-frontend-new` 细化）
- **Chat 挂件**：SSE 流式渲染、阶段进度、知识来源折叠展示、点赞/踩、转人工按钮（Phase2 生效）。
- **客服后台**：会话列表、实时接入（Phase2）、工单处理（Phase2）。
- **管理后台**：知识库管理（上传/删除/状态）、用户管理、配额查看、**真实统计**（不建空壳）。
- **登录/注册**：表单 + 鉴权态管理。

---

## ⑤ 数据规划

- **建模**：`users / sessions / messages / knowledge_bases / documents / chunks / feedback / quotas / tickets*` / `message_sources`（统一真源）。**全表含 `tenant_id`**（MVP 固定单值）。
- **迁移**：Alembic（AegisDesk 无迁移脚本是坑，本项目从 day1 建）。
- **向量存储**：Qdrant collection，payload 存 `tenant_id/doc_id/chunk_id/source`，按知识库/文档过滤用 payload filter。
- **缓存**：Redis 存会话上下文热数据 + 配额计数。
- **文件**：上传原文件落本地卷/对象存储，解析入库即删临时。
- **合规**：用户对话日志加密存储，保留期可配；百炼 Key 不在任何落库字段出现。

---

## ⑥ 部署规划

- **形态**：先支持**单租户私有化**（Docker Compose 一键起：api + worker + postgres + qdrant + redis + frontend + litellm 网关）。
- **CI/CD**：GitHub Actions —— lint → 类型检查 → 单测 → 构建镜像 → 部署 staging（修 AegisDesk 无 CI 的坑）。
- **模型部署**：LiteLLM 网关容器，百炼 API Key 由运维经 secret/KMS 注入；`.env.example` 只放占位、不放真实值。
- **监控**：OTel trace + 错误率/时延面板 + 配额告警。
- **回滚**：镜像 tag 化，出问题一键回退上一个 tag。
- **成本**：LLM 按 token 计费，前端展示预估；知识库批量嵌入走异步 worker 限流。

---

## ⑦ 实施顺序与里程碑（依赖驱动 + DoD 可测）

### 团队角色分工（草案）
| 角色 | 人数 | 职责 |
|---|---|---|
| 项目经理(PM) | 1 | 需求/里程碑/验收，本规划书 owner |
| 后端工程师 | 2 | API、RAG 管线、知识库、工单 |
| 前端工程师 | 1–2 | 挂件/后台/管理台 |
| AI 工程师 | 1 | 检索/重排/提示词/评测集 |
| DevOps | 1 | 部署/CI/监控/模型网关 |
| QA | 1 | 测试基线/评测/回归 |

### MVP（4–6 周，单租户 + 仅 Web + 百炼）
- **W1–W2**：脚手架（FastAPI+PG+Redis+前端）+ 鉴权 + 会话 CRUD + SSE 回声链路 + LiteLLM 接百炼打通。
  - *DoD*：能注册登录、发消息、SSE 收到流式回显；单测覆盖 auth/session；百炼 chat 调用成功。
- **W3–W4**：知识库上传(.txt/.md/.pdf) + 切片 + 向量化(百炼 embedding) + 检索/重排(百炼 rerank)管线 + 流式问答。
  - *DoD*：上传 50 篇文档后，针对文档提问，faithfulness>85%；PDF 可解析（修 AegisDesk 坑）。
- **W5–W6**：反馈闭环（赞/踩+真实 DB source）、配额、基础管理后台、Docker 一键部署 + CI。
  - *DoD*：CI 绿；docker-compose up 后端到端跑通；管理后台能管知识库。

### Phase 2（4–5 周）：多渠道 + 工单
- 企业微信/邮件渠道适配器；WebSocket 在线/转人工信令。
- 工单服务：AI 升级 → 建轻量工单 → 状态机 → 客服后台工单视图与分配。
- 多轮上下文增强 + 业务意图兜底过滤强化。
- *DoD*：企业微信收到消息能自动答，答不了平滑转工单并出现在客服后台；工单状态流转正确。

### Phase 3（4–6 周）：规模化与可观测
- 多租户（启用 `tenant_id` 行级过滤 + 租户隔离测试）；评测体系（RAGAS 基线冻结）；性能优化（检索 P95<200ms）；运营统计（真实数据）。
- *DoD*：多租户隔离测试通过；评测集回归全绿；压测达标。

---

## ⑧ 验收与评测（数据墙，可复现 + 基线冻结）

| 指标(KPI) | 目标 | 测量方式 |
|---|---|---|
| 首字时延 P95 | < 800ms | OTel trace |
| 完整回答时延 P95 | < 2s | OTel trace |
| 检索召回@5 | > 85% | 固定评测集（100 问） |
| 答案 Faithfulness（基于知识、不胡编） | > 90% | RAGAS / 人工抽检 |
| 会话自助解决率(Containment) | > 60% | 未转人工/总会话 |
| 转人工率 | < 40% | 工单/转人工计数 |
| 知识来源可点击率 | 100% | 每条回答必带 DB source |
| CSAT | > 4.2/5 | 赞/踩 + 评分 |
| 工单自动创建准确率(Phase2) | > 85% | 抽样核对 |
| 系统可用性 | 99.5% | 健康检查 |
| 知识导入成功率 | > 99% | 导入日志 |
| 单测覆盖率 | > 70% | CI 报告 |

> **基线冻结**：W4 末冻结一版评测集 hash + 指标门槛，后续只升不降（防 metric-gaming）。

---

## ⑨ 风险与红线（对照 AegisDesk 真实坑 + 历史教训）

| 风险 | 来源/教训 | 应对 |
|---|---|---|
| 配置默认值三处自相矛盾 | AegisDesk：`rag_rewrite_query_count` 2vs3、`MYSQL_DB_NAME` 大小写不一致、embedding 模型 3-small vs v4 | 单一 env 真源 + 启动校验 + 配置单测 |
| 知识来源双轨脆弱 | AegisDesk："模型手写 + source 事件"两路 | 统一真源=数据库 `message_sources`，生成时只引用 DB 来源 |
| PDF 不支持 | AegisDesk `NotImplementedError` | MVP 即接 pdfplumber/unstructured，单测覆盖 |
| 无测试/CI/单维护者 | AegisDesk 致命弱点 | day1 建 CI + 单测基线 + 至少 2 后端 |
| 空壳管理统计 | AegisDesk `/admin/stats` 空实现 | 不做无数据面板；统计必须有真实查询 |
| 密钥/endpoint 硬编码 | AegisDesk `.env.example` 含真实 Qdrant cloud | Secret 管理（KMS/网关），禁止提交真实密钥 |
| 提示词注入 | 通用 + Customer-Agent 教训 | 不可信数据显式标注+转义；端点信任策略 |
| 向量库依赖单点 | Qdrant 宕机=全站失明 | 健康探活 + 降级（稀疏检索兜底）+ 备份 |
| 范围蔓延（代理目标） | 规划教训 | 严守①④范围红线；Phase 外需求进 backlog |
| 检索质量不达标 | RAG 通病 | 评测集驱动；重排失败自动降级 + 兜底过滤 |
| 多租户隔离泄漏 | Phase 3 引入 | MVP 预留 `tenant_id`；Phase 3 做隔离回归测试后才上线 |

---

## ⑩ 附录 ADR（决策记录，均已收敛）

- **ADR-1 向量库 → Qdrant（独立部署）**。理由：AegisDesk 已验证混合检索；检索与关系库压力解耦；payload 过滤干净。代价：多一个容器。
- **ADR-2 租户模型 → 单租户 MVP + 预留 `tenant_id`，Phase 3 多租户**。理由：MVP 上多租户会让每张表/每条检索多一层隔离逻辑，工期 +2~3 周且易出 P0 泄漏；预留字段把迁移成本降到"加过滤条件"。
- **ADR-3 LLM → 阿里云百炼（用户提供 API Key）+ LiteLLM 网关**。chat=通义千问，embedding=百炼 text-embedding，rerank=百炼 gte-rerank。数据不出境、合规、中文强、成本可控；网关抽象保留可切换。Key 经 secret 管理，禁硬编码。
- **ADR-4 渠道 → MVP 仅 Web 挂件，Phase 2 加企业微信/邮件**。理由：最快验证 RAG 主链路质量；渠道是消息搬运工，接口已抽象，后置不返工。
- **ADR-5 工单 → Phase 2 自建轻量工单（状态机 + 客服后台），预留 `TicketAdapter` 外部接口**。理由：与对话同库同登录态、最可控、不依赖外部；以后对接飞书/Jira/TAPD 只加实现。

---

## ⑪ 决策收敛总结（grill-me 输出）

| 分支 | 决策 | 影响 |
|---|---|---|
| ① 租户 | 单租户 MVP，预留 `tenant_id` | 全表加字段；Phase 3 启用隔离 |
| ② 向量库 | Qdrant | docker 多一容器；检索抽象不变 |
| ③ LLM | 百炼 + LiteLLM 网关（用户供 Key） | 数据不出境；Key 走 secret |
| ④ 渠道 | MVP 仅 Web | Phase 1 聚焦问答质量 |
| ⑤ 工单 | Phase 2 自建轻量工单 | 预留外部适配器 |

> 下一步：基于本规划书，进入 `/plan-backend-new` + `/plan-frontend-new` 细化各模块，再用 `/module-dispatch-new` 派发实现。
