# 智能 AI 客服系统（灵犀 / Lingxi）· 前端细规划

> 版本：v0.2-fe（Frontend Detailed，基于规划书 v0.2 决策收敛版）
> 参照：`规划书-智能客服系统.md`（ADR-1~5 锁定）、`后端规划-智能客服系统.md`（API 契约单一真源）
> 范围：**MVP（Web 挂件 + 客服/管理后台）全量细化**；Phase2（企业微信挂件、WebSocket 在线接手、工单视图）仅预留路由与接口位。
> 用途：供 `/module-dispatch-new` 派发 `frontend-builder` 子 agent 按 FE-01~FE-05 逐单元实现。

---

## 0. 范围与边界

- **已锁定**：React 19 + TypeScript + Ant Design 5 + Vite；仅 Web 渠道；SSE 流式问答。
- **MVP 三块界面**：① 可嵌入的**对话挂件**（终端用户用）；② **客服后台**（会话/工单，工单 Phase2）；③ **管理后台**（知识库/用户/真实统计）。
- **契约单一真源**：后端《后端规划》§3 的 request/response schema 与 SSE 事件协议。前端类型定义从契约**派生**，禁止手写漂移。

---

## 1. 架构（依赖顺序：先）

### 1.1 路由表（含守卫）

| 路径 | 页面 | 守卫 | 说明 |
|---|---|---|---|
| `/login`, `/register` | 登录/注册 | 匿名 | 认证后跳 `/chat` 或 `/admin` |
| `/widget` | 对话挂件（嵌入态） | 匿名/普通用户 | 可经 iframe 嵌入第三方页；轻量无侧栏 |
| `/chat` | 客服/用户对话页 | access | 完整态挂件（非嵌入） |
| `/agent/sessions` | 客服会话列表 | agent/admin | Phase2 实时接手 |
| `/agent/tickets` | 工单列表 | agent/admin | **Phase2** |
| `/admin/knowledge` | 知识库管理 | admin | 上传/删除/状态 |
| `/admin/users` | 用户管理 | admin | 角色改 |
| `/admin/stats` | 运营统计 | admin | 真实数据 |

> 挂件两种形态：`/widget`（iframe 嵌入，最小 chrome）与 `/chat`（站内完整页）。共用 `ChatWidget` 组件，差异仅在容器外壳。

### 1.2 组件树（分层：pages / containers / pure）

```
App
├─ Router
├─ layouts/
│   ├─ AuthLayout           (登录/注册外壳)
│   ├─ AdminLayout          (侧栏+顶栏，admin/agent)
│   └─ WidgetShell          (嵌入式最小外壳，无侧栏)
├─ pages/
│   ├─ LoginPage / RegisterPage
│   ├─ ChatPage             (站内对话)
│   ├─ WidgetPage           (嵌入对话)
│   ├─ AgentSessionsPage *  / AgentTicketsPage *   (Phase2)
│   └─ admin/ KnowledgePage / UsersPage / StatsPage
├─ containers/              (接数据，含 React Query)
│   ├─ ChatContainer        (SSE 流 + 会话状态)
│   ├─ KnowledgeContainer   (上传/列表/删除)
│   ├─ UsersContainer
│   └─ StatsContainer
├─ components/              (纯展示，可复用)
│   ├─ chat/ MessageList, MessageBubble, SourceAccordion, StageIndicator,
│   │        ThumbsBar, Composer, StreamingCursor
│   ├─ knowledge/ UploadDropzone, DocTable, StatusTag
│   ├─ common/ AppHeader, SideNav, ErrorBoundary, EmptyState, LoadingSpin
│   └─ charts/ StatCard, TrendChart   (管理统计)
```

### 1.3 状态管理（全局 vs 局部）

- **全局（Zustand）**：`authStore`（token/user/role，持久化 localStorage）、`themeStore`（light/dark/system）。
- **服务端状态（React Query）**：所有 REST 列表/详情（sessions/knowledge/users/stats），缓存 + 失效策略。
- **局部（组件内 useState/useReducer）**：当前会话消息流、SSE 增量缓冲、Composer 输入、Accordion 展开态。
- **SSE 流状态**：`ChatContainer` 内维护 `streamState: {stage, tokens, sources, messageId, error}`；`EventSource` 或 `fetch+ReadableStream` 消费 `/chat/stream` 事件。

### 1.4 接口契约（从后端派生）

> 前端 `src/contracts/` 放 TypeScript 类型，与《后端规划》§3 一一对应：

```ts
// contracts/api.ts —— 字段名/枚举值与后端完全一致
export type Role = 'admin' | 'agent' | 'user';
export interface LoginReq { account: string; password: string }
export interface AuthResp { user_id: string; access_token: string; refresh_token: string }
export interface Session { id: string; title?: string; created_at: string; updated_at: string }
export interface Message { id: string; session_id: string; role: 'user'|'assistant'; content: string; created_at: string }
export interface MessageSource { chunk_id: string; doc_title: string; snippet: string; score: number }
// SSE 事件
export type SSEEvent =
  | { event:'stage'; data:{stage:'retrieving'|'generating'|'done'|'error'; msg?:string} }
  | { event:'token'; data:{delta:string} }
  | { event:'sources'; data:{sources:MessageSource[]} }
  | { event:'done'; data:{message_id:string} }
  | { event:'error'; data:{code:string; message:string} };
export interface DocItem { doc_id:string; name:string; status:'parsing'|'embedding'|'indexed'|'failed'; chunks:number; error?:string }
export interface QuotaResp { date:string; used:number; limit:number; left:number }
export interface AdminStats { sessions:number; messages:number; documents:number; feedback_up:number; feedback_down:number; avg_first_token_ms:number }
```
> 任何字段变更必须先改后端契约，再同步此处；禁止前端自定义字段名。

---

## 2. 布局（依赖顺序：中）

### 2.1 设计 Token（4–6 命名 hex + CSS 变量，含 light/dark）

> 排版即记忆点，避免模板化配色。以下为初始提案，可在实现期微调，但语义变量名固定。

```css
:root {
  --color-brand: #2F6BFF;      /* 主色：信任蓝，用于主按钮/链接/激活态 */
  --color-brand-soft: #E8F0FF; /* 主色浅底，气泡/高亮 */
  --color-accent: #14C8A0;     /* 辅助色：薄荷绿，阶段进度/成功 */
  --color-surface: #FFFFFF;    /* 卡片/输入框底 */
  --color-ink: #1A2233;        /* 主文字 */
  --color-muted: #6B7689;      /* 次要文字/占位 */
  --color-danger: #F5455C;     /* 错误/删除 */
  --radius-md: 12px;
  --shadow-card: 0 2px 12px rgba(26,34,51,.06);
}
[data-theme="dark"] {
  --color-surface: #1B1F2A;
  --color-ink: #E8ECF2;
  --color-muted: #9AA4B5;
  --color-brand-soft: #1E2A45;
  --shadow-card: 0 2px 12px rgba(0,0,0,.35);
}
```
> 主题切换：`<html data-theme>` + AntD `theme.darkAlgorithm` 同步；切换瞬时、无闪烁。

### 2.2 响应式

- **断点**：mobile `<768`（挂件全屏）、tablet `768–1024`（后台侧栏可收起）、desktop `>1024`（完整布局）。
- 移动优先；后台在 mobile 侧栏转抽屉。

### 2.3 关键页面组件设计（含加载/空/错误态）

- **ChatWidget（挂件核心）**：
  - 结构：顶栏（标题+清空+主题切换）→ MessageList（气泡，assistant 带 SourceAccordion + ThumbsBar）→ StageIndicator（检索中/生成中）→ Composer。
  - 流式：token 事件追加到当前 assistant 气泡，`StreamingCursor` 闪烁；sources 事件渲染 Accordion；done 锁定气泡。
  - 空态：欢迎语 + 示例问题；错误态：`event:error` → 顶部告警条 + 重试按钮。
- **KnowledgePage**：UploadDropzone（拖拽/点击，accept `.txt,.md,.pdf`）→ DocTable（名/状态 Tag/块数/删除）；状态 `failed` 红 Tag + hover 看 error。
- **StatsPage**：StatCard 网格（会话/消息/文档/赞踩比/首字时延）+ TrendChart（按日）；数据来自 `/admin/stats` 真实查询。
- **Login/Register**：AntD Form，校验规则；提交走 `authStore` 存 token 并跳转。

---

## 3. 代码（依赖顺序：后）

### 3.1 技术栈

| 项 | 选型 | 理由 |
|---|---|---|
| 框架 | React 19 + TypeScript | 规划书锁定；AegisDesk 验证 |
| 构建 | Vite 5 | 快、HMR；比 CRA 轻 |
| UI | Ant Design 5 | 组件全、中文文档好 |
| 数据请求 | Axios + React Query | 缓存/重试/失效 |
| 全局状态 | Zustand | 轻量、无 Provider 嵌套 |
| SSE | `fetch` + `ReadableStream` 自定义 hook（或 `eventsource` polyfill） | 原生 EventSource 不支持 POST+body，故用 fetch 流 |
| 测试 | Vitest + React Testing Library + Playwright | 渲染测试 + 截图验收 |
| 格式化 | Prettier + ESLint | 统一风格 |

### 3.2 目录与规范

```
frontend/
├─ src/
│  ├─ contracts/      # 类型，从后端派生（单一真源）
│  ├─ api/            # axios 实例 + 各模块 client（auth/sessions/chat/knowledge/...）
│  ├─ hooks/          # useChatStream(SSE), useAuth, useTheme
│  ├─ store/          # zustand: authStore, themeStore
│  ├─ layouts/ pages/ containers/ components/
│  ├─ styles/         # tokens.css, globals.css
│  └─ tests/          # *.test.tsx + playwright e2e
├─ index.html  vite.config.ts  tsconfig.json  package.json  playwright.config.ts
```
- 命名：组件 PascalCase，hook `useXxx`，文件 kebab-case；API client 函数动词开头（`listSessions`/`sendMessage`）。
- 契约优先：新增接口先改 `contracts/`，再写 client，禁止在组件里手写请求体字段。

### 3.3 测试策略

- **渲染测试（Vitest + RTL）**：MessageBubble/SourceAccordion/StageIndicator 渲染正确；空/错误态；登录表单校验。
- **路由可达**：未登录访问 `/admin` 重定向 `/login`；admin 访问 `/admin` 成功。
- **SSE 验收（Playwright 截图）**：`/widget` 发问 → 截图 stage→token→sources→done 全过程；来源 Accordion 可见。截图作为验收证据（页面无法命令输出，用截图+渲染测试对抗"假绿"）。
- **契约一致性**：CI 用 `tsc --noEmit` 保证 `contracts/` 与各 client 类型一致。

---

## 4. 可派发单元（FE-01 ~ FE-05）

### FE-01 前端脚手架（W1）
- **描述**：Vite + React19 + TS + AntD5 + 路由 + axios + React Query + Zustand + 主题系统 + 三套 Layout 空壳 + Vitest/Playwright 配置。
- **文件**：`frontend/{src/,index.html,vite.config.ts,tsconfig.json,package.json,playwright.config.ts,.env.example}`
- **依赖**：无（与 BU-01 并行）。
- **DoD**：`npm run dev` 起；三 Layout 可路由切换；light/dark 切换瞬时；`tsc` 通过。

### FE-02 认证页（W1）
- **描述**：Login/Register 表单 + `authStore`（token 持久化）+ 路由守卫 + 重定向。
- **文件**：`pages/LoginPage.tsx`, `pages/RegisterPage.tsx`, `store/authStore.ts`, `api/auth.ts`, `hooks/useAuth.ts`
- **依赖**：FE-01, 后端 BU-02。
- **DoD**：注册登录后跳转；未登录访问受保护路由重定向；token 过期刷新；表单校验。

### FE-03 对话挂件（W2–W4，核心）
- **描述**：`ChatWidget` 组件 + `ChatContainer` + `useChatStream`（SSE hook，消费 stage/token/sources/done/error）+ MessageList/Bubble/SourceAccordion/StageIndicator/ThumbsBar/Composer + `/widget` 与 `/chat` 两形态。
- **文件**：`components/chat/*`, `containers/ChatContainer.tsx`, `hooks/useChatStream.ts`, `pages/ChatPage.tsx`, `pages/WidgetPage.tsx`, `api/chat.ts`
- **依赖**：FE-01, FE-02, 后端 BU-03/BU-06/BU-07。
- **DoD**：SSE 事件顺序与契约一致；回答带可点击来源（100%）；赞/踩写库；流式光标流畅；空/错误态正确；Playwright 截图通过。

### FE-04 管理后台（W5–W6）
- **描述**：AdminLayout + 知识库管理（上传/列表/删除/状态）+ 用户管理（角色）+ 真实统计（StatCard+TrendChart）。
- **文件**：`layouts/AdminLayout.tsx`, `pages/admin/*`, `containers/KnowledgeContainer.tsx`, `containers/UsersContainer.tsx`, `containers/StatsContainer.tsx`, `components/knowledge/*`, `components/charts/*`, `api/knowledge.ts`, `api/admin.ts`
- **依赖**：FE-01, FE-02, 后端 BU-04/BU-09。
- **DoD**：上传 .txt/.md/.pdf 显示状态机；删除生效；stats 显示真实数字（与后端一致）；非 admin 访问 403 重定向。

### FE-05 契约 & 共享基础（贯穿，W1 起）
- **描述**：`contracts/` 类型定义（与后端同步）+ 通用组件（ErrorBoundary/EmptyState/LoadingSpin/AppHeader/SideNav）+ 主题 token.css + 全局样式。
- **文件**：`contracts/api.ts`, `styles/tokens.css`, `components/common/*`
- **依赖**：FE-01。
- **DoD**：`contracts/api.ts` 覆盖后端全部 MVP schema；通用组件被各页复用；`tsc --noEmit` 全绿。

> **Phase2 预留（不在 MVP 派发）**：`/agent/sessions` 实时接手（WebSocket）、`/agent/tickets` 工单视图、`/widget` 企业微信嵌入形态。接口位已在路由表与契约预留。

---

## 5. 前后端契约对齐清单（派发前自检）

- [ ] `contracts/api.ts` 字段/枚举 == 后端 §3（Role/Session/Message/MessageSource/DocItem/Quota/AdminStats）
- [ ] SSE 事件名（stage/token/sources/done/error）与后端 §3.3.1 完全一致
- [ ] 错误模型 `{code,message,request_id}` 前端有统一拦截
- [ ] 端点路径前缀 `/api/v1` 一致
- [ ] 主题 token 不冲突 AntD 暗色算法
