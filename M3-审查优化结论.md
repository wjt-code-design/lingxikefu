# M3 审查与优化结论（2026-08-15）

## 一、本轮做了什么

### 1. FE-03 浏览器验收（Playwright 端到端）
- 起真实环境（vite 5173 + 后端 8003）用 Playwright + chromium 快照驱动 `/widget`、`/chat` 双形态
- **验证通过**：页面渲染、用户/AI 气泡、SSE 事件管道（stage→token→error→done）、错误处理均正常
- **发现并修复前端 bug**：`useChatStream` 用相对 `API_PREFIX` 发 SSE → 打到 vite dev server 404；改为绝对 `VITE_API_BASE`（提交 `0a775db`）
- 截图：`fe-03-widget.png` / `fe-03-chat.png`

### 2. 漏答题（faithfulness 83.3% 缺口）审查
对 16 个 FAIL 题做检索侧诊断（top_k=8 vs 20 双测）：

| 结论 | 证据 |
|---|---|
| **16 个全是生成缺口** | top1 分数 0.39~0.68，答案全部在已召回 chunks 中 → **检索调参无收益** |
| 失败模式（均无幻觉） | 5 个边缘表述差异（窗口交集 20-29%）+ 4 个漏要点（<20%）+ 3 个数字未全中 + 4 个误拒答 |

### 3. 优化杠杆实证（两个方向都否决）
| 方向 | 结果 | 处置 |
|---|---|---|
| prompt 放宽 | 诚实性 8/8→6/8、Q028 重新编 ¥30（此前已回滚） | ❌ 维持严格红线 |
| **换更强模型 glm-5.2** | qa 2/12 修复，但 **Q028 重新编造 ¥30 上门费（幻觉回归）+ 诚实性 8/8→7/8**（Q071） | ❌ 否决 |

**规律确认**：更强的生成能力 = 更爱编造。glm-5.2 与 prompt 放宽呈现完全相同的回归模式（拒答不彻底 + 编数字）。

## 二、最终结论

- **qa faithfulness 83.3% 是防幻觉约束下的模型完整性上限**，接受为已知限制（差距全部是无幻觉的漏答/表述差异）
- **诚实性 8/8 = 100%**、**引用合法率 ≈96.7%**、**真幻觉 0** —— 三条 grounded-ai 红线保持达标
- 不再尝试 prompt 放宽或模型升级（两条路都已实证回归）

## 三、审查中发现的环境/配置问题（建议处理）

| 问题 | 影响 | 建议 |
|---|---|---|
| `.env` 的 `ZHIPU_API_KEY` 是中文占位符（"你的Key"，len5） | 后端 HTTP 走 zhipu 必炸 ascii/400 | 把真实 key（当前在 `ZHIPUAI_API_KEY` 环境变量，len49）填入 `.env` |
| `DASHSCOPE_API_KEY` 返回 403 | bailian/qwen 通道不可用 | 检查 key 是否过期/配额 |
| torch 多线程段错误（exit 139） | 进程数/线程数触顶时新进程加载 embedding 必崩 | 临时绕法：`OMP_NUM_THREADS=1` + `torch.set_num_threads(1)` |

## 四、提交与产物

- 提交链：`61889e1 → b59ece7 → b382346 → e7565f1 → 0a775db`
- 工作树干净（仅报告与截图未提交）
- 遗留：① 4000 端口起过 8001/8002/8003 测试实例（未清理干净）；② 8000 上仍是旧版后端进程（缺 /sessions、/chat/stream 路由），正式验收需用最新代码重启
