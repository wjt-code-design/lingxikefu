# 智能 AI 客服系统（灵犀 / Lingxi）· 后端细规划

> 版本：v0.2-be（Backend Detailed，基于规划书 v0.2 决策收敛版）
> 参照：`规划书-智能客服系统.md`（ADR-1~5 已锁定）
> 范围：**MVP（W1–W6）全量细化**；Phase 2/3 仅预留接口与数据模型，不展开实现。
> 用途：供 `/module-dispatch-new` 派发 `backend-builder` 子 agent 按 BU-01~BU-09 逐单元实现。

---

## 0. 范围与边界（开工前必读）

- **已锁定（不可改）**：FastAPI + LangGraph + PostgreSQL16 + Qdrant + React19/AntD5 + LiteLLM→阿里云百炼；单租户 MVP、全表 `tenant_id`；MVP 仅 Web 渠道；Phase2 自建工单。
- **MVP 必须交付**：auth / sessions / chat(SSE) / knowledge(.txt/.md/.pdf) / rag 管线 / feedback / quota / 基础 admin / docker-compose / CI。
- **Phase 2 仅预留**：ticket 表 + `external_ref` + `TicketAdapter` 抽象；channel 适配层接口 + WebSocket 信令接口。
- **红线（来自规划书⑨）**：配置单一 env 真源 + 启动校验；知识来源唯一真源=DB `message_sources`（废弃"模型手写 source"双轨）；PDF 必须支持；day1 有 CI + 单测；禁止提交真实密钥/endpoint。

---

## 1. 统一领域术语（ubiquitous language）

> 术语不定，接口必乱。后端所有模块、前端、评测集共用以下定义。

| 术语 | 定义 | 易混点澄清 |
|---|---|---|
| **Tenant** | 租户。MVP 固定单值（如 `default`），Phase3 多租户时启用行级过滤。 | 不是"客户"，是"部署实例的归属方" |
| **User** | 系统账号（管理员/客服/普通用户三角色）。 | 区别于"终端咨询者"——终端咨询者 anonymous 或绑定普通用户 |
| **Session** | 一次多轮对话会话，归属一个 User 与一个（可选）终端咨询者。 | 非 WebSocket 连接；连接可断，Session 持久 |
| **Message** | 会话内的一条消息（user/assistant），带 role 与 metadata。 | assistant 消息的"知识来源"在 `message_sources`，不内联 |
| **MessageSource** | assistant 回答引用的知识块（chunk_id + 文档标题 + 片段）。**唯一真源**。 | 取代 AegisDesk 的"模型手写 source 字符串" |
| **KnowledgeBase** | 知识库，文档与向量的容器，可独立授权/过滤。 | 一个租户可有多个 KB |
| **Document** | 上传的原始文件（.txt/.md/.pdf），含解析状态机。 | 一文件多 chunk |
| **Chunk** | 切片后的文本单元，落 Qdrant（payload: tenant_id/doc_id/chunk_id/source）+ 关联 Document。 | 检索的最小单位 |
| **Intent** | 意图分类结果（问答/转人工/闲聊/业务意图关键词）。 | 业务意图关键词用于兜底过滤 |
| **Quota** | 每日提问配额（独立表 + 行锁）。 | 非 User 字段，防绕过 |
| **Ticket\*** | 工单（Phase2）。AI 判定转人工→建单→状态机。 | 预留，MVP 不实现 |
| **Channel\*** | 接入渠道（Web/企业微信/邮件）。MVP 仅 Web（HTTP+SSE）。 | 预留适配层接口 |

---

## 2. 架构分层（依赖单向向下）

```
config → models → repositories → llm_clients(embedding/rerank/chat via LiteLLM)
       → retrieval(dense+sparse+RRF) → rag_pipeline(intent→rewrite→retrieve→rerank→filter→expand→generate)
       → orchestrator(LangGraph state machine) → api(routers) + workers(Celery: import)
```

- **config**：Pydantic Settings，单一 env 真源 + 启动校验（`settings.validate()` 在 main 启动时调用）。
- **models**：SQLAlchemy 2.0 声明式，全表含 `tenant_id`。
- **repositories**：每实体一个 repo，封装 DB 操作，业务层不写裸 SQL。
- **llm_clients**：chat/embedding/rerank 三个 client，统一走 LiteLLM（`litellm.completion/academic_embeddings/rerank`），Key 由网关注入。
- **retrieval**：Qdrant 查询构造 + 稀疏向量生成（沿用 AegisDesk 的 MD5 hash + log 加权，无词表依赖）+ 外层 RRF 融合。
- **rag_pipeline**：纯函数式管线节点，便于单测与评测。
- **orchestrator**：LangGraph `StateGraph`，7 节点（intent→rewrite→retrieve→rerank→filter→expand→generate），节点间传 `AgentState`。
- **api**：FastAPI router 分组（auth/sessions/chat/knowledge/feedback/quota/admin）；中间件做 JWT + 配额检查。
- **workers**：Celery 消费文档导入任务（解析→切片→向量化→入库），失败回滚清向量。

---

## 3. API 设计（schema 单一真源）

> **契约真源**：本节的 request/response schema 即前后端共享契约。前端 `contracts/` 类型定义从本节派生，**禁止手写漂移**（规划书 PL#13）。
> 统一前缀 `/api/v1`，统一错误模型 `{ "code": string, "message": string, "request_id": string }`。
> 错误码段：`AUTH_*` / `PARAM_*` / `KB_*` / `QUOTA_*` / `RAG_*` / `SYS_*`。

### 3.1 Auth
| 方法 | 路径 | 请求 | 响应 | 鉴权 |
|---|---|---|---|---|
| POST | /auth/register | `{email?, phone?, password, role?}` | `{user_id, access_token, refresh_token}` | 否 |
| POST | /auth/login | `{account, password}` | `{user_id, access_token, refresh_token}` | 否 |
| POST | /auth/refresh | `{refresh_token}` | `{access_token}` | refresh |
| POST | /auth/logout | `{}` | `{ok:true}` | access |
| GET | /auth/me | — | `{user_id, email?, phone?, role, quota_left}` | access |

### 3.2 Sessions
| 方法 | 路径 | 请求 | 响应 | 鉴权 |
|---|---|---|---|---|
| GET | /sessions | `{page,size}` | `{items:[{id,title,created_at,updated_at}],total}` | access |
| POST | /sessions | `{title?}` | `{id,title,created_at}` | access |
| GET | /sessions/{id} | — | `{id,title,messages:[...]}` | access(owner/admin) |
| DELETE | /sessions/{id} | — | `{ok:true}` | access(owner/admin) |

### 3.3 Chat（SSE）
| 方法 | 路径 | 请求 | 响应 | 鉴权 |
|---|---|---|---|---|
| POST | /chat/stream | `{session_id, content, stream:true}` | `text/event-stream`，事件见 3.3.1 | access + 配额扣减 |

**3.3.1 SSE 事件协议（前端契约单一真源）**
```
event: stage        data: {"stage":"retrieving"|"generating"|"done"|"error","msg?":str}
event: token        data: {"delta": str}              # 流式增量
event: sources      data: {"sources":[{chunk_id,doc_title,snippet,score}]}  # 最终来源
event: done         data: {"message_id": str}
event: error        data: {"code": str,"message": str}
```
> 阶段顺序：retrieving → generating → token* → sources → done。错误统一 `event: error`。

### 3.4 Knowledge
| 方法 | 路径 | 请求 | 响应 | 鉴权 |
|---|---|---|---|---|
| POST | /knowledge-bases | `{name, description?}` | `{kb_id,name}` | admin |
| GET | /knowledge-bases | — | `{items:[{kb_id,name,doc_count,chunk_count}]}` | admin |
| POST | /knowledge-bases/{kb_id}/documents | `multipart: file` | `{doc_id,status:"parsing"}` | admin |
| GET | /knowledge-bases/{kb_id}/documents | — | `{items:[{doc_id,name,status,chunks,error?}]}` | admin |
| DELETE | /documents/{doc_id} | — | `{ok:true}` | admin |
| POST | /documents/{doc_id}/reindex | — | `{job_id:"queued"}` | admin |

`Document.status` 枚举：`parsing → embedding → indexed | failed`。失败带 `error`。

### 3.5 Feedback
| 方法 | 路径 | 请求 | 响应 | 鉴权 |
|---|---|---|---|---|
| POST | /messages/{message_id}/feedback | `{rating:"up"|"down", comment?}` | `{ok:true}` | access |

### 3.6 Quota
| 方法 | 路径 | 请求 | 响应 | 鉴权 |
|---|---|---|---|---|
| GET | /quota/me | — | `{date, used, limit, left}` | access |

### 3.7 Admin
| 方法 | 路径 | 请求 | 响应 | 鉴权 |
|---|---|---|---|---|
| GET | /admin/users | `{page,size}` | `{items:[{user_id,account,role,created_at}],total}` | admin |
| PUT | /admin/users/{id}/role | `{role}` | `{ok:true}` | admin |
| GET | /admin/stats | `{from,to}` | `{sessions, messages, documents, feedback_up, feedback_down, avg_first_token_ms}` | admin（**真实查询，禁空壳**） |

> Phase2 预留：`POST /tickets`、`GET /tickets`、`WS /ws/agent`（在线/转人工信令）、`POST /channels/wecom/webhook`。

---

## 4. 数据模型（全表含 `tenant_id`）

### 4.1 实体与关键字段
- **users**：`id, tenant_id, email?, phone?, password_hash, role(enum:admin|agent|user), created_at, status`
- **sessions**：`id, tenant_id, user_id, title?, created_at, updated_at`
- **messages**：`id, tenant_id, session_id, role(enum:user|assistant), content, created_at, intent?, meta(jsonb)`
- **message_sources**（唯一真源）：`id, tenant_id, message_id, chunk_id, doc_id, doc_title, snippet, score`
- **knowledge_bases**：`id, tenant_id, name, description?, created_at`
- **documents**：`id, tenant_id, kb_id, name, status(enum), sha256, chunk_count, error?, created_at`
- **chunks**：`id, tenant_id, doc_id, kb_id, idx, text, hash` + Qdrant payload 镜像（`chunk_id/doc_id/tenant_id/source`）
- **feedback**：`id, tenant_id, message_id, user_id, rating(enum:up|down), comment?, created_at`
- **quotas**（每日）：`id, tenant_id, user_id, date(date), used(int), limit(int)`，唯一约束 `(tenant_id,user_id,date)`，行锁更新
- **tickets\***（预留）：`id, tenant_id, session_id, status(enum:open|processing|resolved|closed), assignee_id?, external_ref?, created_at, updated_at`
- **chunk_context\***（预留，相邻块扩展）：`chunk_id, prev_id?, next_id?`

### 4.2 关系
`users 1—N sessions`；`sessions 1—N messages`；`messages 1—N message_sources`；`knowledge_bases 1—N documents`；`documents 1—N chunks`；`kb 1—N chunks`；`messages 1—1 feedback`（每 assistant 消息至多一票）。

### 4.3 迁移
- **Alembic 从 day1 建**（AegisDesk 无迁移是坑）。首版迁移含全表 + `tenant_id` 索引 + quotas 唯一约束。
- 每次 schema 变更走 `upgrade()`/`downgrade()` 双写。

### 4.4 缓存（Redis）
- `session:ctx:{session_id}` → 最近 N 条消息（上下文窗口热数据，TTL 30min）。
- `quota:{tenant_id}:{user_id}:{date}` → 计数（与 DB 双写，DB 为权威）。
- `ratelimit:ip:{ip}` → 滑动窗口计数。

---

## 5. 并发与性能（每个失败路径有兜底）

- **配额扣减**：`UPDATE quotas SET used=used+1 WHERE ... FOR UPDATE`（行锁），超限返回 `QUOTA_EXCEEDED`，**fail-closed**（锁失败=拒绝，不默认放行）。
- **SSE 流式**：LLM 调用一律 `await anyio.to_thread.run_sync` 或 `async def` 原生异步，避免阻塞事件循环；生成器 `async def` yield token。
- **向量化批处理**：embedding 批量调用 + `asyncio.Semaphore(8)` 限并发；单条失败重试 2 次后退化为"仅 sparse 检索"，不阻断问答（**降级显式，不静默**）。
- **Qdrant 单点**：健康探活（`/health` 定时）；宕机时检索降级为 sparse-only 并打 `SYS_DEGRADED` 标记，回答标注"检索能力降级"。
- **导入任务**：Celery worker 异步，大文件切片并发受限流；导入失败整体回滚（删已写 chunk + 清 Qdrant 向量 + 文档置 `failed`）。
- **限流**：API 层双窗口（固定窗口计数 + 滑动日志），超限 `429`。

---

## 6. 安全

- **认证**：JWT（access 15min / refresh 7d），`AuthMiddleware` 解析并注入 `current_user`；refresh 走独立端点。
- **授权**：三角色（admin/agent/user）。admin 仅能访问 `/admin/*`；agent 可访问 `/sessions` 全部（Phase2 接手）；user 仅自己资源。越权返回 `403 AUTH_FORBIDDEN`。
- **提示词注入防护**（沿用 Customer-Agent 教训）：所有不可信输入（用户消息、网页抓取）进入 prompt 前：① 显式分隔标注 `<<<USER_INPUT>>>`；② 全角转义敏感字符；③ 业务意图关键词兜底过滤（防"忽略上文指令"类攻击）。
- **信任边界**：XFF 仅取受信代理层第一个值，禁止前端伪造 IP 绕过限流；所有 LLM 调用经 LiteLLM 网关，**前端禁止直连模型**。
- **密钥管理**：百炼 Key 仅经网关 env/KMS 注入；`.env.example` 只放占位 `LITELLM_MASTER_KEY=__CHANGE_ME__`；CI secret 扫描（gitleaks）拦截真实密钥。
- **日志脱敏**：用户消息/PII 落日志前脱敏；trace 保留但字段 masking。

---

## 7. 测试策略（真后端，禁内存假绿）

- **单元**：rag_pipeline 各节点纯函数单测（intent/rewrite/rerank/filter/expand）；retrieval RRF 融合公式；quota 行锁逻辑。
- **集成**：真 PostgreSQL + 真 Qdrant（docker 起），不做 sqlite/内存 fake：① 上传→解析→入库→检索命中；② chat SSE 事件顺序；③ 配额超限拒绝。
- **评测**：固定 100 问评测集（Phase W4 冻结 hash），CI 跑 faithfulness/recall@5，门槛只升不降。
- **失败路径必测**：PDF 解析失败回滚、embedding 重试耗尽降级、Qdrant 不可达降级、JWT 过期、越权访问。

---

## 8. 可派发单元（BU-01 ~ BU-09）

> 每个单元独立可派发；`依赖` 顶部的先完成。DoD 即验收门槛。

### BU-01 项目脚手架（W1）
- **描述**：FastAPI 应用骨架 + PostgreSQL + Redis + Celery + Alembic + docker-compose + GitHub Actions CI（lint→type→单测→构建镜像）。
- **文件**：`backend/{app/,tests/,alembic/,Dockerfile,docker-compose.yml,pyproject.toml,.env.example,requirements.txt}`
- **依赖**：无。
- **DoD**：`docker-compose up` 起 api+pg+redis+qdrant+worker；`/health` 返回 ok；CI 绿（含配置启动校验单测）。

### BU-02 Auth 模块（W1）
- **描述**：register/login/refresh/logout/me；JWT 签发与中间件；三角色；密码 bcrypt。
- **文件**：`app/api/auth.py`, `app/core/security.py`, `app/models/user.py`, `app/repositories/user_repo.py`
- **依赖**：BU-01。
- **DoD**：注册登录拿 token；`/auth/me` 返回角色；越权 403；单测覆盖 5 端点 + 过期刷新。

### BU-03 Session 模块（W1）
- **描述**：多轮会话 CRUD + 上下文窗口（Redis 热数据）+ 历史查询。
- **文件**：`app/api/sessions.py`, `app/models/session.py`, `app/repositories/session_repo.py`, `app/services/session_service.py`
- **依赖**：BU-02。
- **DoD**：建/列/查/删会话；上下文正确维护；owner 隔离。

### BU-04 Knowledge 模块（W2–W3）
- **描述**：KB CRUD；文档上传（.txt/.md/**pdf**）；解析（pdfplumber+markdown/unstructured）；切片（text_splitter）；向量化（百炼 embedding）；sha256 去重；状态机；导入失败回滚。Celery worker 异步。
- **文件**：`app/api/knowledge.py`, `app/services/knowledge_import_service.py`, `app/services/document_service.py`, `app/utils/text_splitter.py`, `app/workers/import_worker.py`
- **依赖**：BU-01, BU-08(向量 client)。
- **DoD**：上传 50 篇（含 PDF）入库；sha256 重复上传跳过；PDF 解析成功（修 AegisDesk 坑）；失败回滚清向量；单测覆盖 txt/md/pdf + 回滚。

### BU-05 RAG 管线（W3–W4，核心）
- **描述**：LangGraph 7 节点状态机：intent→query rewrite(多 query)→hybrid retrieve(dense+sparse)→RRF→rerank(百炼 gte-rerank，失败降级)→业务意图关键词兜底过滤→相邻块扩展→generate(百炼 Qwen)。检索/重排抽象层，便于换模型。
- **文件**：`app/orchestrator/graph.py`, `app/services/rag_service.py`, `app/services/vector_service.py`, `app/services/rerank_service.py`, `app/services/intent_service.py`, `app/services/sparse_service.py`, `app/services/embedding_service.py`, `app/prompts/qa_prompt.py`
- **依赖**：BU-04, BU-08。
- **DoD**：针对文档提问 faithfulness>85%、recall@5>85%；阶段事件顺序正确；rerank 失败自动降级且回答可用；注入攻击被兜底过滤拦截。

### BU-06 Chat 模块（W2–W4，SSE）
- **描述**：接收消息→写库→调 orchestrator→SSE 流式回传（stage/token/sources/done/error 事件）→保存 assistant 消息 + 写 `message_sources` 真源 + 配额扣减。
- **文件**：`app/api/chat.py`, `app/services/chat_service.py`
- **依赖**：BU-03, BU-05, BU-07, BU-09。
- **DoD**：SSE 事件顺序与 3.3.1 一致；回答带 DB 来源（100% 可点击）；配额超限拒答；首字 P95<800ms（本地）。

### BU-07 Feedback 模块（W5）
- **描述**：赞/踩 + 评论；写 `feedback` 表；来源以 `message_sources` 为准（**单真源**）。
- **文件**：`app/api/feedback.py`, `app/models/feedback.py`, `app/repositories/feedback_repo.py`
- **依赖**：BU-02。
- **DoD**：投票写库；重复投票幂等；来源来自 DB 非模型手写。

### BU-08 Quota 模块（W5）
- **描述**：每日配额表 + 行锁扣减 + Redis 计数；`/quota/me`。
- **文件**：`app/models/quota.py`, `app/services/quota_service.py`, `app/api/quota.py`
- **依赖**：BU-02。
- **DoD**：超限拒绝（fail-closed）；并发扣减无超卖（行锁单测）；`/quota/me` 正确。

### BU-09 Admin 模块（W5–W6）
- **描述**：用户管理（角色改）、知识库管理视角、真实统计（`/admin/stats` 真实查询，禁空壳）。
- **文件**：`app/api/admin.py`, `app/services/admin_service.py`
- **依赖**：BU-02, BU-04, BU-07。
- **DoD**：admin 能管用户/知识库；stats 返回真实数字（与 DB 一致）；非 admin 访问 403。

> **MVP 收尾**：BU-01~09 完成 = W1–W6 达成。**Phase2 单元**（ticket/channel/ws）与 **Phase3**（多租户隔离、评测体系）不在此派发，待 MVP 验收后启动。

---

## 9. 交付检查清单（派发前自检）

- [ ] 所有模块全表含 `tenant_id`，MVP 固定单值不报错
- [ ] API schema 与前端 `contracts/` 一致（见《前端规划》§契约）
- [ ] 配置单一真源 + 启动校验单测存在
- [ ] 知识来源唯一真源=DB，无模型手写 source
- [ ] PDF 支持 + 单测覆盖
- [ ] CI 绿 + 单测基线 >70%
- [ ] 密钥/endpoint 未提交（gitleaks 通过）
