# 统一文本换行符为 LF：消除 Windows(CRLF) 跨平台漂移
# 背景：契约双源(contracts/api.ts vs frontend/src/contracts/api.ts)曾因 CRLF/LF 出现整文件假 diff
* text=auto eol=lf

# 常见二进制显式声明，防止 git 误判为文本做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
