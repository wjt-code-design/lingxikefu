# 灵犀（Lingxi）智能客服 · 深度审计报告（第二轮）

> 审计对象：`frontend/`、`backend/`、`contracts/`（`wt/`、`research/` 为无关目录，未纳入）
> 方法：四路并行代码审计（聊天核心链路 / 页面设计与 UX / 契约数据层 / 后端安全）+ 对关键结论逐条人工复核
> 输入：《issues-prioritized-lingxi.md》（下称"原清单"）十条主张
> 结论先行：**原清单十条中 7 条属实、2 条已过时或需修正、1 条表述要更精确**；另发现 **28 项**原清单未覆盖的问题，其中 4 项建议升至 P0。

---

## 一、对原优化建议书的复核（大胆质疑部分）

| 条目 | 原结论 | 复核结果 | 证据 |
|---|---|---|---|
| ① 契约漂移 | 属实 | ✅ **属实，且比原描述更严重**：根契约缺 15+ 组类型（详见 §四.1） | 前端 36 处 import 全指 `@/contracts/api`，无一引用根契约 |
| ② 断连配额白扣 | 属实 | ✅ 属实，但"assistant 不落库"是设计（未生成完不写），真正的 bug 是 **RAG 失败/断连时 user 消息与配额扣减均不回滚、也不补写 error 消息** | `chat.py:268` 预扣、`:276-284` user 先 commit、`:299/:318/:325` 断连仅 return |
| ③ done message_id 恒空 | 属实 | ⚠️ **已过时**：`chat.py:359-390` 已在 chat 层**拦截** rag 层的空 done，落库后发出**带真实 message_id 的 done**，前端拿到的 id 是真实的。残留问题只剩：rag_service 内部仍发空 done（协议分层脏）、根契约没更新、前端本地消息 id 与后端 id 双轨 | `chat.py:366-368` `_persist_answer` → `:387` 真 id；`useChatStream.ts:38-44` 正常消费 |
| ④ 交接全靠轮询 | 属实 | ⚠️ **已过时**：前端已实现 SSE 通知长连接 + 轮询兜底，并非"纯轮询"。原修法方向 D（SSE 通知通道）**大部分已落地** | `notifications.ts:46` `subscribeNotifications`（fetch+ReadableStream，断线重连由调用方兜底） |
| ⑤ 意图纯规则 | 属实 | ✅ 属实 | `rag_service.py:35-45` 三组关键词、`:70-76` `any(k in query)` |
| ⑥ 多 Agent 名实不符 | 属实 | ✅ 属实 | `orchestrator/__init__.py` 仅 1 行 docstring |
| ⑦ 空占位目录 | 属实 | ✅ 属实 | 两目录仅 `__init__.py` |
| ⑧ repo 模式不彻底 | 属实 | ✅ 属实 | 仅 `document_repo.py`/`users.py`；`auth.py:26`、`quota.py:25`、`knowledge_import_service.py:44` 直 ORM |
| ⑨ ChatStage 无后端持久化 | 属实 | ✅ 属实 | `ChatStage` 仅定义于 `useChatStream.ts:6` |
| ⑩ eval 未接 CI | 属实 | ✅ 属实 | `.github/workflows/` 全仓零命中 |

**对原清单的总体评价**：方向和优先级基本靠谱，但它是基于较早的代码状态写的——③④两条描述的缺陷**现在已经被修掉了大半**，若照单施工会做重复工作。另外它完全没覆盖**安全**和**前端聊天组件生命周期**这两个维度，恰是本轮发现最集中的地方。

---

## 二、新发现 · 前端聊天链路（原清单未覆盖）

### 🔴 C1. 流式连接无卸载清理，切会话会"串台"
- **位置**：`hooks/useChatStream.ts` 全文件无 `useEffect` cleanup（已验证）；`ChatContainer.tsx` 仅 sources 同步和 finalize 用了 effect
- **问题**：`abortRef` 只在"发新消息"和"finalize"时 abort。组件卸载（路由跳走）或生成中切换历史会话时，旧 fetch 继续跑、token 继续追加，finalize 时把**旧会话的回答写进新会话的消息列表**
- **影响**：消息串台、连接泄漏、对已卸载组件 setState
- **修法**：hook 内加 `useEffect(() => () => abortRef.current?.abort(), [])`；finalize 时比对发起时的 sessionId 与当前 sessionId，不一致则丢弃

### 🔴 C2. 快速连发会丢回答
- **位置**：`ChatContainer.tsx:126-155`（finalize effect）+ `:157-197`（onSend）
- **问题**：`streamingUserRef` 是单值 ref，第一条还在流式中就发第二条时，ref 被覆盖；第一条的 done 到达后按错误的 uid 落账，第一条 assistant 回答可能丢失
- **修法**：Composer 在 streaming 时已 disabled（`ChatContainer.tsx:329`），但"热门问题卡片"（`:276-283`）只 disabled creating 不 disabled streaming，是漏网点；建议 finalize 携带本轮 uid 闭包而非共享 ref

### 🟡 C3. 中文输入法回车误发送
- **位置**：`Composer.tsx:42-46`（已验证）：`onPressEnter` 未检查 `e.nativeEvent.isComposing`
- **影响**：中文用户按回车确认候选词的瞬间把半句话发出去——客服场景用户几乎全中文，这是**高频可感知的体验 bug**
- **修法**：`if (!e.shiftKey && !e.nativeEvent.isComposing)` 一行修复

### 🟡 C4. 本地消息 id 与后端 message_id 双轨
- **位置**：`ChatContainer.tsx:133`（已验证）assistant 本地 id 用 `a-${Date.now()}`，后端真 id 存在另一字段 `messageId`（`:138`）
- **影响**：刷新后历史加载用后端 id，本地 id 体系作废；未来做"引用/编辑/定位到某条消息"时两套 id 对不上
- **修法**：done 后以 messageId 为准覆盖本地 id（现在 done 已带真 id，见 §一.③）

### 🟡 C5. SSE 解析脆弱 + 失败静默
- **位置**：`useChatStream.ts`（按 `\n\n` 切帧、只取首个 `data:` 行、JSON.parse 失败直接 continue）；`notifications.ts:67-76` 同款写法
- **影响**：当前后端 `_sse` 单行输出所以没炸；一旦后端加多行 data 或心跳穿插就丢 token，且无日志难排查
- **修法**：累积所有 `data:` 行拼接后再 parse；parse 失败 `console.warn`

### 🟢 C6. 流式每 token 触发 smooth 滚动
- **位置**：`MessageList.tsx` 滚动依赖 `tokens.length`，每 token 一次 `scrollIntoView({behavior:'smooth'})`
- **影响**：高速吐字时动画排队抖动；阈值内的上滑会被拉回，打断阅读
- **修法**：rAF 节流 + 用户上滑超过阈值（如 80px）后暂停 auto-stick，出现"回到底部"按钮

---

## 三、新发现 · 前端页面设计（用户重点关注区）

### 🔴 D1. 注册链路冗余且密码策略过弱
- **位置**：`RegisterPage.tsx:25-30`（已验证）：注册成功后**再次调用 login** 换 token（两次串行请求）；`:78-81` 密码规则仅 `min: 6`，无复杂度、无强度指示
- **影响**：弱网下注册成功但二次登录失败 → 用户以为注册失败重来，可能产生重复账号；6 位纯数字密码可过，不符合客服系统安全基线
- **修法**：后端 register 直接返回 token（一次请求）；密码至少"8 位 + 字母数字"，加 AntD 强度条

### 🟡 D2. 部分数据页错误态无重试入口
- **位置**：`pages/admin/SettingsPage.tsx` 错误态只有文字"读取失败，请稍后重试"；`ProfilePage.tsx` 未处理 `isError`
- **修法**：封装统一的 `QueryErrorState`（文案 + refetch 按钮），全站替换

### 🟡 D3. AdminLayout 无响应式断点
- **位置**：`layouts/AdminLayout.tsx` Sider 固定 `width={220}`，无 `breakpoint`/`collapsedWidth`
- **影响**：平板竖屏内容区被挤压；widget 嵌入方若在小屏 iframe 引用后台页会很难用
- **修法**：`breakpoint="lg"` + `collapsedWidth={64}`

### 🟡 D4. 已封装的品牌组件未被复用（设计不一致）
- **位置**：`EmptyState`/`BrandEmpty` 已存在，但 `TicketsPage`、`CustomersPage` 等表格空态仍用 AntD 默认 Empty；`ProfilePage.tsx:41`（gold/cyan/blue）、`AppTable.tsx:31-43`（STATUS_COLOR）直接用 AntD 预设色名，绕过 `tokens.css` 体系
- **修法**：`AppTable` 内部统一注入品牌空态；状态色映射到设计 token

### 🟢 D5. 占位链接语义错误
- **位置**：`LoginPage.tsx`"忘记密码"、`RegisterPage.tsx`"服务条款"：`href="#"` + toast
- **修法**：改 `<Button type="link">` 或补 `aria-disabled`，避免屏幕阅读器误判

### 🟢 D6. 表格轮询刷新可能重置滚动位置
- **位置**：`TicketsPage`/`CustomersPage`/`LogsPage` 使用 react-query 轮询；通知到达触发 invalidate 时 AntD Table 重绘
- **修法**：高频列表开 `virtual`，或将通知到达改为精准缓存更新

### ✅ 页面设计做得好的（复核确认，勿误伤）
- **刷新不会踢回登录页**：`main.tsx:11` 在 createRoot **之前** `await bootstrapAuth()` 静默续期，token 不落 localStorage 是刻意的安全设计（BUG-15 注释）——这点初查曾误报，已澄清
- **通知面板有滚动约束**：`globals.css:1415-1418` 有 `max-height: 420px; overflow-y: auto`（初查误报，已澄清）
- 三态覆盖规范、ErrorBoundary 带 sendBeacon 上报、403/404 有出路、token 刷新共享 Promise 防并发、通知 SSE+30s 轮询双保险、设计 token 双层语义（User/Workbench）

---

## 四、新发现 · 契约 / 数据层

### 🔴 A1. 根契约缺失清单（比原清单①描述更具体）
根 `contracts/api.ts` + `api-schema.json` 缺失而前端已在用的类型：
`RefreshResp.refresh_token`、`Session.satisfaction/user_email/user_phone`、`MessageSource.doc_id`、**Ticket 整组**、**Customer 整组**、`SSEEvent.intent` 与 `done.ticket_id`、`DocumentListResp`、**KnowledgeSearch 整组**、**Faq 整组**、`AdminStats.hot_gaps`、`RoleDef`、**AdminSettings 整组**、**AuditLog 整组**、**TrendPoint 整组**、**Notification 整组**
→ 契约收敛时这些是必须回填的 diff 清单。

### 🟡 A2. 根目录两份真源自相矛盾
- `api-schema.json` 的 `RegisterReq` 有 `anyOf[email|phone]` 至少一个必填约束，`api.ts` 两者皆 optional → 前端可提交空注册吃 422
- `MeResp.quota_left`：schema=`integer`，TS=`number`

### 🟡 A3. schema 纯手写、无同步机制
后端代码中无任何 schema 生成/校验逻辑（grep 零命中），`api-schema.json` 与 Pydantic 模型无绑定 → 建议用 FastAPI 的 `app.openapi()` 自动生成并 CI diff。

### 🟢 A4. 协议卫生
- 分页不统一：`SessionListReq` page/size 必填 vs `AuditLogListReq` 可选 vs `KBListResp` 无 total → 建议统一分页泛型
- `NotifySSEEvent`（connected/notification/ping）只定义在前端，契约文件均未收录
- 时间字段全是裸 `string`，无 ISO/date-only 区分

---

## 五、新发现 · 后端安全与正确性（原清单完全未覆盖的维度）

### 🔴 S1. Redis 故障 → 认证安全机制整体失效（fail-open）
- **位置**：`core/token_revocation.py:25-26 / :42-44 / :53-55`（已验证）：吊销写入、refresh 防重放（SETNX）、黑名单查询三处异常时**全部降级放行**
- **影响**：Redis 抖动期间，已登出用户的 token 依然有效；同一 refresh token 可并发换发多枚新 token
- **修法**：至少 refresh 端点 fail-closed（Redis 不可用即拒绝换发）；或进程内内存黑名单兜底

### 🔴 S2. 工单状态流转无并发保护
- **位置**：`tickets.py:248-269`（已验证）：读取→改状态→commit，无 version/updated_at 乐观锁条件
- **影响**：两个客服同时操作同一工单，后者静默覆盖前者；审计日志与实际状态可能对不上
- **修法**：UPDATE 带 `WHERE updated_at = 旧值` 乐观锁，0 行即 409

### 🟡 S3. 注册角色无 DB 约束兜底
`auth.py:37-40` 仅应用层校验 role；未来新增注册入口忘校验即可造出 admin。建议加 CHECK 约束或强制注册端点只写 `user`。

### 🟡 S4. 知识库导入线程 daemon=True
进程退出时 embedding 线程被强杀，文档状态永久卡在 parsing/embedding，无恢复机制。

### 🟡 S5. 性能债
- `sessions.updated_at`、`tickets.status` 缺索引（高频排序/过滤走全表扫描）
- `admin.py` stats 把全量 assistant 消息 meta 拉进 Python 内存聚合，量大 OOM
- `sessions.py:159-163` 取会话消息无分页上限
- `database.py:33-39` `get_db()` 异常时不 rollback，脏连接归还池

### 🟢 S6. 依赖与配置卫生
- `requirements.txt` 的 `python-jose` 维护停滞且有已知 CVE，建议迁 PyJWT/authlib
- `config.py:20-22` KNOWN_WEAK_SECRETS 硬编码在源码
- `auth.py:34-38` 信任 X-Forwarded-For 首段，反代未固定时可伪造 IP 绕登录限流

---

## 六、重排后的修复优先级（合并原清单 + 本轮发现）

**P0（计费/数据正确性/安全，建议一周内）**
1. **聊天生命周期收口**：stream 卸载/切会话 abort（C1）+ finalize 绑 sessionId（C2）+ 原清单②的幂等键/成功才扣费 + RAG 失败补写 error 消息消除孤儿 user——四件事同一个改动面，一次做完
2. **Redis fail-closed**（S1）
3. **契约收敛**：根契约按 §四.1 清单回填 → 前端改 import 根契约 → `app.openapi()` 生成 schema + CI diff（原① + A1/A2/A3）
4. **工单乐观锁**（S2）

**P1（高频体验，建议两周内）**
5. 输入法误发（C3，一行修复，性价比最高）
6. 注册一步返 token + 密码强度（D1）
7. 流式滚动节流 + 上滑保护（C6）
8. 错误态统一重试组件（D2）
9. 索引补齐 + stats 聚合下推 DB（S5）

**P2（长期健康）**
10. 设计一致性：品牌空态/色彩 token 收敛（D4）
11. AdminLayout 响应式（D3）、SSE 解析器加固（C5）
12. eval 接 CI（原⑩）、python-jose 替换（S6）
13. 原⑥文档对齐、⑤意图路由按原清单建议 E 评估（注意：④的 SSE 通道已存在，只需补断线重连策略与契约收录）

---

## 七、总体评价

项目底子中上：契约先行、三态覆盖、token 安全设计（不落 localStorage + 启动续期）、通知双通道、错误边界上报这些工程纪律都在线，后端无 SQL 注入/路径穿越，归属校验覆盖面广。**真正的风险集中在两处**：聊天流式生命周期（C1/C2 + 原②）和契约漂移（原①）——前者直接产生用户可感的消息丢失/串台，后者是所有前后端不一致的根源。原清单的优先级框架可用，但需按本报告修正③④两条过时结论，并把 S1/S2 两个安全问题补进 P0。

---

*审计方式：四路子代理并行代码审计 + 关键结论人工复核（已剔除 2 条初查误报：刷新踢登录、通知面板溢出）。所有问题均标注 `文件:行号` 可直接跳转。未改动项目任何文件。*
